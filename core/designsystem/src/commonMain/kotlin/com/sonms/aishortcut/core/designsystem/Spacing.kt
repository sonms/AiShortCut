package com.sonms.aishortcut.core.designsystem

import androidx.compose.ui.unit.dp

// 4dp grid from DESIGN.md section 3. Plain object, not a CompositionLocal:
// the scale is fixed and every screen reads the same values.
object Spacing {
    val xs = 4.dp
    val sm = 8.dp
    val md = 16.dp
    val lg = 24.dp
    val xl = 32.dp
}
